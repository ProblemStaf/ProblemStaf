#! server-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: server
    namespace: messenger
    labels:
        com.docker.compose.project: messenger
        com.docker.compose.service: server
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: messenger
            com.docker.compose.service: server
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: messenger
                com.docker.compose.service: server
                com.docker.compose.network.default: "true"
        spec:
            containers:
                - name: server
                  image: messenger-server
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: JWT_SECRET
                      value: "your_jwt_secret"
                    - name: MONGO_URI
                      value: "mongodb://mongodb:27017/messenger"
                  ports:
                    - name: server-3001
                      containerPort: 3001
